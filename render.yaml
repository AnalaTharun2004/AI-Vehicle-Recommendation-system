services:
  - type: web
    name: ai-vehicle-recommendation
    runtime: python
    buildCommand: pip install -r requirements.txt && python scripts/init_database.py
    startCommand: gunicorn app:app
    healthCheckPath: /